<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_riderhub"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_riderhub_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".InformeActivity">

        </activity>

        <activity android:name=".MessageActivity">
            <!-- Pueden haber más configuraciones aquí -->
        </activity>

        <activity android:name=".NewPublicationActivity">
            <!-- Pueden haber más configuraciones aquí -->
        </activity>

        <activity android:name=".ProfileActivity">
            <!-- Pueden haber más configuraciones aquí -->
        </activity>

        <activity android:name=".SearchActivity">
            <!-- Pueden haber más configuraciones aquí -->
        </activity>

        <activity android:name=".Search2Activity">
            <!-- Pueden haber más configuraciones aquí -->
        </activity>

        <activity android:name=".LogInActivity">
        </activity>

            <activity android:name=".RegisterActivity">
            </activity>
    </application>

</manifest>